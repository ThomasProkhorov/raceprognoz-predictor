{
  "name": "raceprognoz-predictor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter raceprognoz-parser build && bun run --filter raceprognoz-web build",
    "postinstall": "cd apps/raceprognoz-web && playwright install chromium"
  }
}